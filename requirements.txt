# ============================================================
# 24/7 YouTube Live Stream â€” System Requirements
# ============================================================
# These are installed automatically by the GitHub Actions workflow.
# If running locally on Ubuntu/Debian, install with:
#   sudo apt-get install -y $(grep -v '^#' requirements.txt | tr '\n' ' ')
# ============================================================

# Core streaming engine
ffmpeg

# JSON parser (used in stream.sh to read playlist.json)
jq

# Font package (required by FFmpeg drawtext filter for ticker)
fonts-dejavu-core
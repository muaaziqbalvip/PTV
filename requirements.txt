# ============================================================
# 24/7 YouTube Live Stream — System Requirements
# ============================================================
# Auto-installed by GitHub Actions workflow.
# Local install: sudo apt-get install -y ffmpeg jq fonts-dejavu-core
# Python deps:   pip install yt-dlp
# ============================================================

# Core streaming engine
ffmpeg

# JSON parser (used in stream.sh)
jq

# Font package (required by FFmpeg drawtext/ticker)
fonts-dejavu-core

# YouTube URL resolver (Python package)
# Install: pip install yt-dlp --break-system-packages
yt-dlp